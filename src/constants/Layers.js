import defaultOptions from "../config";

const Orto = {
    ICGC5M: defaultOptions.urlTerrainICGC[0].url,
    WORLD3M: defaultOptions.urlTerrainICGC[1].url  
  };

  const Vector = {
    ICGC5M: defaultOptions.urlTerrainICGC[0].url,
    WORLD3M: defaultOptions.urlTerrainICGC[1].url  
  };  

export default {Orto,Vector};